// Função a ser completada
function soma(numero1, numero2) {
        return numero1 + numero2;
      }
      const resultado = soma(800, 840);
      console.log(resultado);
    // Completar a função para retornar a soma de 800 e 840

